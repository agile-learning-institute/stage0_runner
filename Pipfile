[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]

[scripts]
validate = "python src/command.py validate --runbook ${RUNBOOK:-./samples/runbooks/SimpleRunbook.md}"
execute = "python src/command.py execute --runbook ${RUNBOOK:-./samples/runbooks/SimpleRunbook.md}"

[requires]
python_version = "3.12"

